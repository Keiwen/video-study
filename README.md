# video-study

Simple webpage to read videos from the server (PHP required).
Place videos in any sub-folder (default 'videos/').
If sub-folder name changed,
adjust the PHP constant at the start of the index file

As a user, you can select a videos among the list and study it.
There are quick shortcuts to adjust video speed
(arrow up and down).
